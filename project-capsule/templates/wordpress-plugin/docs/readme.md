# WordPress Plugin

Starter voor een WordPress plugin. Hoofdbestand zit in `code/plugin.php`.

## Ontwikkeling

1. Plaats de `code/` map in `wp-content/plugins/` (of symlink).
2. Activeer de plugin in de WP admin.
